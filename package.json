{
  "name": "weekly-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Internal weekly planner for kids activities and transport schedules.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "start": "npm run build && node ./scripts/serve.mjs dist"
  }
}
